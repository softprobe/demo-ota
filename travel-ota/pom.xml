<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.0</version>
        <relativePath/>
    </parent>

    <groupId>org.example</groupId>
    <artifactId>travel-ota</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>travel-ota</name>
    <description>机票预订系统</description>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Spring Boot 3.2.0 管的 lombok 1.18.30 在 JDK 21.0.7 下会崩(TypeTag :: UNKNOWN),
             因为 javac 21.0.4+ 变更了内部 TypeTag 枚举,lombok 反射访问失败。1.18.36
             (2024-12 发布)明确支持 JDK 23,兼容 JDK 21 全部 patch 版本。这里显式覆盖
             parent 管的版本以修复 -parameters/getter 生成崩溃。 -->
        <lombok.version>1.18.38</lombok.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Web Starter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Jackson for JSON processing -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Lombok for reducing boilerplate code -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Shared airline models -->
        <dependency>
            <groupId>com.airline</groupId>
            <artifactId>airline-common</artifactId>
            <version>1.0.0</version>
        </dependency>

        <!-- ============================================================
             数据层 — 让 travel-ota 有真实的 DB + Redis 下游,这样 SP agent
             录制入口时的 trace 里能同时看到 JDBC SQL span、Redis span、HTTP
             span 三种下游,回放时全部 mock。全内嵌、自包含:H2 内存库 + 进程
             内 embedded redis,不给 demo 加任何集群级 Redis/MySQL 基础设施,
             配合 demo pod 单副本 + 每晚重置(内存数据随 pod 重启丢正好)。
             ============================================================ -->
        <!-- H2 内存库:agent 拦 java.sql/JDBC → 录 SQL 下游 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Redis 客户端(Lettuce):agent 拦客户端调用 → 录 Redis 下游 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <!-- 进程内 embedded redis-server(localhost:6379)。base image
             eclipse-temurin:21-jre-jammy 是 glibc,内置二进制兼容;codemonstur
             版兼容 Java 21 / Spring Boot 3。 -->
        <dependency>
            <groupId>com.github.codemonstur</groupId>
            <artifactId>embedded-redis</artifactId>
            <version>1.4.3</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- 显式声明 lombok 处理器路径。JPA/Redis 依赖引入后,plexus-compiler
                 内嵌的 annotation processor 自动发现路径失效(lombok 官方 issue
                 #1723 / #2839,Spring Boot 3.x + JPA 组合下的已知问题),导致
                 @Getter/@Setter 生成的方法在同 project 内使用它们的 service 类里
                 全部"找不到符号"。显式给出 processorPath 后 lombok 必定参与编译。 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
